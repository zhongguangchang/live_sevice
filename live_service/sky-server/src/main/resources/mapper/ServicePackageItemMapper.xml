<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ServicePackageItemMapper">

    <insert id="insertBatch">
        insert into service_package_item (service_package_id, service_id, name, price, copies)
        values
        <foreach collection="items" item="item" separator=",">
            (#{item.servicePackageId}, #{item.serviceId}, #{item.name}, #{item.price}, #{item.copies})
        </foreach>
    </insert>

    <delete id="deleteByPackageIds">
        delete from service_package_item where service_package_id in
        <foreach collection="packageIds" item="packageId" open="(" separator="," close=")">
            #{packageId}
        </foreach>
    </delete>

    <select id="listByPackageIds" resultType="ServicePackageItem">
        select * from service_package_item where service_package_id in
        <foreach collection="packageIds" item="packageId" open="(" separator="," close=")">
            #{packageId}
        </foreach>
    </select>

</mapper>
