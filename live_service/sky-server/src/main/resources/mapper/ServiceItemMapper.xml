<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ServiceItemMapper">

    <insert id="insert" parameterType="ServiceItem" useGeneratedKeys="true" keyProperty="id">
        insert into service_item (name, category_id, price, original_price, image, description, detail,
                                  unit, duration, service_mode, need_appoint, sales, score, status,
                                  merchant_id, create_time, update_time, create_user, update_user)
        values (#{name}, #{categoryId}, #{price}, #{originalPrice}, #{image}, #{description}, #{detail},
                #{unit}, #{duration}, #{serviceMode}, #{needAppoint}, #{sales}, #{score}, #{status},
                #{merchantId}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <update id="update" parameterType="ServiceItem">
        update service_item
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="unit != null and unit != ''">unit = #{unit},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="serviceMode != null">service_mode = #{serviceMode},</if>
            <if test="needAppoint != null">need_appoint = #{needAppoint},</if>
            <if test="sales != null">sales = #{sales},</if>
            <if test="score != null">score = #{score},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteByIds">
        delete from service_item where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 分页查询联表带出分类名，规格列表由 Service 层另外查一次再填充 -->
    <select id="pageQuery" resultType="com.sky.vo.ServiceItemVO">
        select si.*, c.name as categoryName
        from service_item si
        left outer join category c on si.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                and si.name like concat('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                and si.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and si.status = #{status}
            </if>
            <if test="serviceMode != null">
                and si.service_mode = #{serviceMode}
            </if>
        </where>
        order by si.create_time desc
    </select>

    <select id="list" parameterType="ServiceItem" resultType="ServiceItem">
        select * from service_item
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="serviceMode != null">
                and service_mode = #{serviceMode}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from service_item
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>

</mapper>
