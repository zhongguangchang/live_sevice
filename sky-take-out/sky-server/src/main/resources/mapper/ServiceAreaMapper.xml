<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ServiceAreaMapper">

    <insert id="insert" parameterType="ServiceArea" useGeneratedKeys="true" keyProperty="id">
        insert into service_area (name, code, city_code, city_name, sort, status, create_time, update_time)
        values (#{name}, #{code}, #{cityCode}, #{cityName}, #{sort}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="ServiceArea">
        update service_area
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="code != null and code != ''">code = #{code},</if>
            <if test="cityCode != null">city_code = #{cityCode},</if>
            <if test="cityName != null">city_name = #{cityName},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <select id="list" parameterType="ServiceArea" resultType="ServiceArea">
        select * from service_area
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="cityCode != null and cityCode != ''">
                and city_code = #{cityCode}
            </if>
        </where>
        order by sort
    </select>

</mapper>
