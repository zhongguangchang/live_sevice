<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ServiceOrderItemMapper">

    <insert id="insertBatch">
        insert into service_order_item (order_id, service_id, service_package_id, name, spec,
                                        price, number, amount, image, duration)
        values
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.serviceId}, #{item.servicePackageId}, #{item.name}, #{item.spec},
             #{item.price}, #{item.number}, #{item.amount}, #{item.image}, #{item.duration})
        </foreach>
    </insert>

    <select id="listByOrderIds" resultType="ServiceOrderItem">
        select * from service_order_item where order_id in
        <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
            #{orderId}
        </foreach>
    </select>

</mapper>
